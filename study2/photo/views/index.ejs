<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <style>
        ul,li{
            list-style: none;
            margin: 0;
        }
        li{
            position: relative;
            float: left;
            margin:15px;
        }
        a{
            display: block;
            position: relative;
            width: 150px;
            height:150px;
            background: deepskyblue;
        }
        li>a>p{
            position: absolute;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,.5);
            color:#fff;
            line-height:40px;
            text-align: center;
            margin: 0;
        }

    </style>
</head>
<body>
<ul>
    <% for(var i = 0;i<album.length;i++){ %>
        <li>
            <a href="./<%= album[i].title%>">
                <p> <%= album[i].title %></p>
            </a>
        </li>
    <% } %>
</ul>
</body>
</html>